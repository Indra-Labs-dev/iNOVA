// Design tokens per docs/03-frontend/design-system.md "Starting palette".
// These are starting values, not immutable brand rules — expect refinement
// once real screens beyond this Phase 0 shell are built.
import 'package:flutter/material.dart';

class INovaColors {
  const INovaColors._();

  static const Color deepSpace = Color(0xFF07111F);
  static const Color electricBlue = Color(0xFF0066FF);
  static const Color cyan = Color(0xFF20D9FF);
  static const Color purple = Color(0xFF8B5CFF);
  static const Color neonOrange = Color(0xFFFF5A1F);
  static const Color white = Color(0xFFF5F8FF);
}
